services:
  api:
    build: ./backend
    ports:
      - "127.0.0.1:8000:8000"
    volumes:
      - memory-mate-data:/data
    environment:
      ALLOWED_ORIGINS: http://localhost:3002,http://127.0.0.1:3002
volumes:
  memory-mate-data:
